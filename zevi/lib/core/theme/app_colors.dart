import 'package:flutter/material.dart';

class AppColors {
  // Dark theme colors (from test.html tailwind.config)
  static const inkBg = Color(0xFF131318);           // surface-dim
  static const inkDeep = Color(0xFF0E0E13);         // surface-container-lowest
  static const surfaceLow = Color(0xFF1B1B20);      // surface-container-low
  static const surface = Color(0xFF1F1F24);         // surface-container
  static const surfaceHigh = Color(0xFF2A292F);     // surface-container-high
  static const surfaceHighest = Color(0xFF35343A);  // surface-container-highest
  static const violet = Color(0xFF6C47FF);          // primary-container
  static const violetDim = Color(0xFFC9BEFF);       // primary / primary-fixed-dim
  static const onSurface = Color(0xFFE4E1E9);       // on-surface
  static const onSurfaceVariant = Color(0xFFC9C3D9);// on-surface-variant
  static const outline = Color(0xFF938EA2);         // outline
  static const outlineVariant = Color(0xFF484556);  // outline-variant
  static const success = Color(0xFF1DB97C);
  static const warning = Color(0xFFF5A623);
  static const danger = Color(0xFFE84545);
  
  // Light theme colors
  static const ashBg = Color(0xFFF4F3F7);
  static const white = Color(0xFFFFFFFF);
}
