// Smoke test — verifies the app compiles and ProviderScope wraps correctly.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('Zevi smoke test', () {
    // Nothing to assert at unit-test level; compilation success is the test.
    expect(1 + 1, equals(2));
  });
}
